<div class="flashes mt-2">
  <% flash.each do |key, value| %>
    <% if key == 'alert' or key == 'notice' %>
      <%= content_tag(:div, value, class: "flash #{key}", id: "#{key}") %>
    <% end %>
  <% end %>
</div>
