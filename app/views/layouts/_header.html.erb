<nav class="static bg-white shadow-[0_2px_15px_-3px_rgba(0,0,0,0.07),0_10px_20px_-2px_rgba(0,0,0,0.04)]" data-controller="dropdown">
  <div class="container mx-auto px-4 sm:px-8">
    <div class="flex justify-between h-16">
      <div class="flex-shrink-0 flex items-center">
        <div class="flex">
          <%= link_to root_path do %>
            <div class="flex flexitems-center">
              <div class="inline-block h-8 justify-self-start content-start items-start self-start">
                <%= image_tag 'logoz.png', alt: 'LSA Logo', height: '35.75', width: '100', class: '' %>
              </div>
              <span class="text-blue-900 font text-3xl px-4">
                Database
              </span>
            </div>
            <div class="hidden sm:ml-6 sm:flex sm:space-x-4 pl-2">
              <div class="header_link">
                <%= link_to "About", static_pages_about_path, class: "#{class_names(active: current_page?(developers_path))}" %>
              </div>
            </div>
            <div class="hidden sm:ml-6 sm:flex sm:space-x-4 pl-2 ">
              <div class="header_link">
                <%= link_to "Developers", developers_path, class: "#{class_names(active: current_page?(developers_path))}" %>
              </div>
            </div>
            <div class="hidden sm:ml-6 sm:flex sm:space-x-4 pl-2">
              <div class="header_link">
                <%= link_to "Web Applications", webapps_path, class: "#{class_names(active: current_page?(developers_path))}" %>
              </div>
            </div>
            <% if user_signed_in? %>
              <div class="hidden sm:ml-6 sm:flex sm:space-x-4 pl-2">
                <button id="dropdownDefaultButton" data-dropdown-toggle="dropdown" class="text-white bg-blue-700 border border-2 hover:bg-blue-800 focus:ring-4 focus:outline-none focus:ring-blue-300 font-medium rounded-lg text-sm px-5 py-2.5 text-center inline-flex items-center dark:bg-blue-600 dark:hover:bg-blue-700 dark:focus:ring-blue-800 data-turbolinks=false" type="button">My Profile <svg class="w-2.5 h-2.5 ml-2.5" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 10 6">
                    <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m1 1 4 4 4-4"/>
                  </svg></button>
                <!-- Dropdown menu -->
                <div id="dropdown" class="z-10 hidden bg-white divide-y divide-gray-100 rounded-lg shadow w-44 dark:bg-gray-700">
                  <ul class="py-2 text-sm text-gray-700 dark:text-gray-200" aria-labelledby="dropdownDefaultButton">
                    <li>
                      <div class="block px-4 py-2 "><%= button_to "Sign out", destroy_user_session_path, method: :delete, class: "#{class_names(active: current_page?(developers_path))}" %></div>
                      <div class="block px-4 py-2">
                        <%= link_to "My Profile", developer_path(current_user.developer) %>
                      </div>
                      <div class="block px-4 py-2">
                        <%= link_to "Edit Profile", edit_developer_path(current_user.developer) %>
                      </div>
                      <% if current_user.has_role? :admin %>
                        <div class="block px-4 py-2">
                          <%= link_to "Role Management", users_path %>
                        </div>
                      <% end %>
                      <div class="block px-4 py-2">
                        <%= link_to "Account Management", edit_user_registration_path %>
                      </div>
                    </li>
                  </ul>
                </div>
              </div>
            <% else %>
              <div class="hidden sm:ml-6 sm:flex sm:space-x-4 pl-2">
                <div class="header_link">
                  <%= link_to "Sign in", new_user_session_path, class: "#{class_names(active: current_page?(developers_path))}" %>
                </div>
              </div>
            <% end %>
          </div>
        <% end %>
      </div>
    </div>
  </nav>
